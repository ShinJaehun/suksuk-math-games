# Rush Hour Standalone

Rails 앱 `ShinJaehun/rushhour`의 `app/views/pages/home.html.erb`에 들어 있던 HTML/CSS/JavaScript 게임 코드를 분리한 버전입니다.

## 실행

`index.html`을 브라우저로 열면 됩니다.

## 파일

- `index.html`: 화면 구조
- `style.css`: 보드, 셀, 자동차 스타일
- `game.js`: 레벨 데이터, 드래그, 충돌 판정, 클리어 판정
